# vm-init
* Before creating new VMs - open Hyper-V and create a new virtual switch called 'multipass' - Private type
* Update multipass-init.yaml with public ssh key for admin user
* Update kubernetes repo version (currently defined as 1.35)
