# kubernetes-cluster-setup
Scripts to deploy a HA Kubernetes cluster with external HA etcd and multi‑control‑plane nodes using Multipass VMs for training purpose
* Start by creating VMs with scripts located in vm-init folder
* Continue with scripts in kubernetes folder
